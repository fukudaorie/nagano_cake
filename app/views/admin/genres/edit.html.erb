<div class = "container">
  <h3 class = "ml-5 mb-5">ジャンル編集</h3>
  
  <%= form_with model: @genre, url: admin_genre_path(@genre) do |f| %>
  <%= f.label "ジャンル名", class: "mr-5" %>
  <%= f.text_field :name, class: "mr-5" %>
  <%= f.submit '変更を保存', class: "btn btn-success" %>
  <% end %>
</div>